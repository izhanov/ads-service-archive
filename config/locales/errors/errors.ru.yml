ru:
  dry_validation:
    errors:
      or: "или"
      array?: "может быть только массивом"

      empty?: "нужно оставить пустым"

      excludes?: "не может включать в себя %{value}"

      excluded_from?:
        arg:
          default: "не может принимать следующие значения: %{list}"
          range: "не может принимать следующие значения: %{list_left} - %{list_right}"
      exclusion?: "не может принимать следующие значения: %{list}"

      eql?: "может иметь только значение, равное %{left}"

      not_eql?: "не может иметь значение, равное %{left}"

      filled?: "не может быть пустым"

      format?: "не может быть в таком формате"

      number?: "может быть только числом"

      odd?: "может быть только нечётным числом"

      even?: "может быть только чётным числом"

      gt?: "может принимать только значения больше, чем %{num}"

      gteq?: "может принимать только значения больше, чем или равные %{num}"

      hash?: "может быть только типом hash"

      included_in?:
        arg:
          default: "может принимать только следующие значения: %{list}"
          range: "может принимать только следующие значения: %{list_left} - %{list_right}"
      inclusion?: "может принимать только следующие значения: %{list}"

      includes?: "не может не включать в себя %{value}"

      bool?: "может быть только типом boolean"

      true?: "может принимать только значениe true"

      false?: "может принимать только значениe false"

      int?: "может быть только целым числом"

      float?: "может быть только типом float"

      decimal?: "может быть только типом decimal"

      date?: "может быть только типом date"

      date_time?: "может быть только типом date time"

      time?: "может быть только типом time"

      key?: "не может отсутствовать"

      attr?: "не может отсутствовать"

      lt?: "может принимать только значения меньше, чем %{num}"

      lteq?: "может принимать только значения меньше, чем или равные %{num}"

      max_size?: "размер не может быть больше %{num} символов"

      min_size?: "размер не может быть меньше %{num} символов"

      none?: "не определяется"

      str?: "может быть только строкой"

      type?: "может быть только типом %{type}"

      slug?: "может содержать только символы a-z, 0-9 и -"

      phone?: "не может быть в таком формате"

      coordinates?: "Координаты не могут быть в таком формате"

      email?: "должен включать в себя @"

      rules:
        primary_phone:
          phone_format: "Телефон указан не верно"

        contact_phone:
          phone_format: "Телефон указан не верно"

        email:
          email_format: "Не верный формат"

        currency:
          currency_format: "Должен соответсвовать ISO4217 (USD, EUR, RUB, etc.)"

        password_confirmation:
          password_confirmation_mismatch: "Пароли не совпадают"

        kind:
          invalid_kind: "Не верный тип соц. сети"

      size?:
        arg:
          default: "размер должен быть равен %{size}"
          range: "размер должен быть в диапазоне %{size_left} - %{size_right}"

        value:
          string:
            arg:
              default: "длина должна быть %{size}"
              range: "длина должна быть в диапазоне %{size_left} - %{size_right}"
